---
import { cn } from "@/lib/utils"

interface Props {
  href: string
  external?: boolean
  class?: string
  underline?: boolean
  [key: string]: any
}

const { href, external, class: className, underline, ...rest } = Astro.props
---

<a
  href={href}
  target={external ? "_blank" : "_self"}
  rel={external ? "noopener noreferrer nofollow" : undefined}
  class={cn(
    "inline transition-colors duration-300 ease-in-out hover:text-accent",
    underline &&
      "underline decoration-muted-foreground underline-offset-[3px] hover:decoration-accent",
    className
  )}
  {...rest}
>
  <slot />
</a>
